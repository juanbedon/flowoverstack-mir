<div class="col">
	<div class="card">
		<div class="row">
			<div class="card-body">
			  <h5 class="card-title"><%= link_to question.title, question %></h5>
			  <h6 class="published-date">Submitted <%= time_ago_in_words(question.created_at) %> ago by <%= question.user.email %></h6>

		    <%= link_to Upvote_question_path(question), method: :put, class: 'btn btn-outline-dark btn_sm' do %>
		    	<i class="fas fa-caret-up"></i>
		    	Upvote
		    	<%= question.get_upvotes.size %>
		  	<% end %>

		    <%= link_to Downvote_question_path(question), method: :put, class: 'btn btn-outline-dark btn_sm' do %>
		    	<i class="fas fa-caret-down"></i>
		    	Downvote
		    	<%= question.get_downvotes.size %>
		  	<% end %>
		  	
			</div>
		</div>
	</div>
</div>

