<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo03" aria-controls="navbarTogglerDemo03" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand" href="/">Flow<span class="light">Overstack</span></a>

    <div class="collapse navbar-collapse" id="navbarTogglerDemo03">
      <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Ask Question', new_question_path, class: "nav-link #{yield(:question_active)}" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Account', edit_user_registration_path, class: "nav-link #{yield(:account_active)}" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Log Out', destroy_user_session_path, method: :delete, class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Log In', new_user_session_path, class: "nav-link #{yield(:login_active)}" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign Up',new_user_registration_path, class: "nav-link #{yield(:signup_active)}" %>
          </li>
        <% end %>
        <form class="form-inline my-2 my-lg-0">
          <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
        </form>
      </ul>
    </div>
  </div>
</nav>